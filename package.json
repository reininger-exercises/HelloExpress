{
  "name": "hello",
  "version": "1.0.0",
  "description": "The goal of this app is to get an express web server deployed as quickly as possible. It only needs to return a simple html page to start.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
